# CI Workflow Fixes Patch

> Generated by **BugFixer ULW Cycle Jun 27 2026** (`fix/bugfixer-jun-27-2026`)

## Status: Unpushed (Permission Blocked)

The workflow fixes below **cannot be pushed** from this runner because the GitHub App token lacks `workflows: write` permission.

## Fixes Included

### BUG-014: Stale Doc References in `main.yml`

| Before | After |
|--------|-------|
| `docs/bug.md, docs/feature.md` | `docs/bugs.md, docs/features.md` |
| `docs/bug.md` | `docs/bugs.md` |

### BUG-017: Node.js Version Mismatch

Changed 11 occurrences of `node-version: "20"`/`node-version: 20` → `node-version-file: ".node-version"`:

| File | Occurrences |
|------|-------------|
| `.github/workflows/iterate.yml` | 5 |
| `.github/workflows/parallel.yml` | 4 |
| `.github/workflows/on-pull.yml` | 1 |
| `.github/workflows/pr-gatekeeper.yml` | 1 |

## How to Apply

```bash
# From repo root:
git apply docs/ci-workflow-fixes-patch.md
```

Or manually:
1. Open each file listed above
2. Replace all `node-version: "20"` (or `node-version: 20`) with `node-version-file: ".node-version"`
3. In `.github/workflows/main.yml`:
   - Line ~39: `docs/bug.md` → `docs/bugs.md`, `docs/feature.md` → `docs/features.md`
   - Line ~263: `docs/bug.md` → `docs/bugs.md`
4. Verify with: `grep -rn "docs/bug\\.md\\|docs/feature\\.md" .github/` and `grep -rn "node-version:" .github/`

## Verification

- Build ✅
- Lint ✅
- Typecheck ✅
- Tests 1,701/1,701 ✅
- Secrets scan ✅

## Root Cause

The `main.yml` and other workflow files are periodically replaced by CI workflows that use older template versions containing stale refs and hardcoded Node.js 20. Without `workflows: write` permission on the GitHub App token, these fixes cannot be pushed to main.
