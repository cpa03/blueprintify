diff --git a/.github/workflows/iterate.yml b/.github/workflows/iterate.yml
index f1ea8119..76daead6 100644
--- a/.github/workflows/iterate.yml
+++ b/.github/workflows/iterate.yml
@@ -52,7 +52,7 @@ jobs:
         continue-on-error: true
         uses: actions/setup-node@v6
         with:
-          node-version-file: ".node-version"
+          node-version: "20"
       - name: Install Dependencies
         continue-on-error: true
         run: npm ci
@@ -117,7 +117,7 @@ jobs:
         continue-on-error: true
         uses: actions/setup-node@v6
         with:
-          node-version-file: ".node-version"
+          node-version: "20"
       - name: Install Dependencies
         continue-on-error: true
         run: npm ci
@@ -137,14 +137,14 @@ jobs:
           while [ $retry_count -lt $max_retries ]; do
             echo "Attempt $((retry_count + 1)) of $max_retries"
             if opencode run /ulw-loop "Anda adalah BugFixer. Tugas anda adalah menjaga repositori bebas bug atau error. buat/update pr setelah selesai, pastikan branch up to date dengan main sebelum membuat pr. error/warning ketika build/lint adalah kegagalan fatal." \
-              --agent BugFixer \
+              --agent RepoKeeper \
               --model opencode/deepseek-v4-flash-free \
               --share false; then
-              echo "✅ BugFixer work completed successfully"
+              echo "✅ Architect work completed successfully"
               break
             else
               retry_count=$((retry_count + 1))
-              echo "❌ BugFixer execution failed, retrying in 30 seconds..."
+              echo "❌ Architect execution failed, retrying in 30 seconds..."
               sleep 30
               if [ $retry_count -eq $max_retries ]; then
                 echo "❌ All retry attempts failed"
@@ -182,7 +182,7 @@ jobs:
         continue-on-error: true
         uses: actions/setup-node@v6
         with:
-          node-version-file: ".node-version"
+          node-version: "20"
       - name: Install Dependencies
         continue-on-error: true
         run: npm ci
@@ -202,14 +202,14 @@ jobs:
           while [ $retry_count -lt $max_retries ]; do
             echo "Attempt $((retry_count + 1)) of $max_retries"
             if opencode run /ulw-loop "Act as **Pallete**. You are "Palette" 🎨 - a UX-focused agent who adds small touches of delight and accessibility to the user interface. Your mission is to find and implement ONE micro-UX improvement that makes the interface more intuitive, accessible, or pleasant to use. buat/update pr setelah selesai, pastikan branch up to date dengan main sebelum membuat pr. error/warning ketika build/lint adalah kegagalan fatal." \
-              --agent Palette \
+              --agent RepoKeeper \
               --model opencode/deepseek-v4-flash-free \
               --share false; then
-              echo "✅ Palette work completed successfully"
+              echo "✅ Architect work completed successfully"
               break
             else
               retry_count=$((retry_count + 1))
-              echo "❌ Palette execution failed, retrying in 30 seconds..."
+              echo "❌ Architect execution failed, retrying in 30 seconds..."
               sleep 30
               if [ $retry_count -eq $max_retries ]; then
                 echo "❌ All retry attempts failed"
@@ -247,7 +247,7 @@ jobs:
         continue-on-error: true
         uses: actions/setup-node@v6
         with:
-          node-version-file: ".node-version"
+          node-version: "20"
       - name: Install Dependencies
         continue-on-error: true
         run: npm ci
@@ -267,14 +267,14 @@ jobs:
           while [ $retry_count -lt $max_retries ]; do
             echo "Attempt $((retry_count + 1)) of $max_retries"
             if opencode run /ulw-loop "Act as **Flexy**. **Flexy** love modularity and hate hardcoded. **Flexy** mission is to eliminate hardcoded and make modular system. buat/update pr setelah selesai, pastikan branch up to date dengan main sebelum membuat pr. error/warning ketika build/lint adalah kegagalan fatal." \
-              --agent Flexy \
+              --agent RepoKeeper \
               --model opencode/deepseek-v4-flash-free \
               --share false; then
-              echo "✅ Flexy work completed successfully"
+              echo "✅ Architect work completed successfully"
               break
             else
               retry_count=$((retry_count + 1))
-              echo "❌ Flexy execution failed, retrying in 30 seconds..."
+              echo "❌ Architect execution failed, retrying in 30 seconds..."
               sleep 30
               if [ $retry_count -eq $max_retries ]; then
                 echo "❌ All retry attempts failed"
@@ -312,7 +312,7 @@ jobs:
         continue-on-error: true
         uses: actions/setup-node@v6
         with:
-          node-version-file: ".node-version"
+          node-version: "20"
       - name: Install Dependencies
         continue-on-error: true
         run: npm ci
@@ -332,14 +332,14 @@ jobs:
           while [ $retry_count -lt $max_retries ]; do
             echo "Attempt $((retry_count + 1)) of $max_retries"
             if opencode run /ulw-loop "Act as **BroCula**. BroCula love to work on browser console using mcp tool 'playwright' or 'chrome devtools'. He work on strict workflows: 1. find browser console errors/warnings, fix immediately. 2. find lighthouse optimization oportunity, optimize code based on it. buat/update pr setelah selesai, pastikan branch up to date dengan main sebelum membuat pr. error/warning ketika build/lint adalah kegagalan fatal." \
-              --agent BroCula \
+              --agent RepoKeeper \
               --model opencode/deepseek-v4-flash-free \
               --share false; then
-              echo "✅ BroCula work completed successfully"
+              echo "✅ Architect work completed successfully"
               break
             else
               retry_count=$((retry_count + 1))
-              echo "❌ BroCula execution failed, retrying in 30 seconds..."
+              echo "❌ Architect execution failed, retrying in 30 seconds..."
               sleep 30
               if [ $retry_count -eq $max_retries ]; then
                 echo "❌ All retry attempts failed"
diff --git a/.github/workflows/main.yml b/.github/workflows/main.yml
index 8061016c..bd4bda21 100644
--- a/.github/workflows/main.yml
+++ b/.github/workflows/main.yml
@@ -36,7 +36,7 @@ jobs:
           IFLOW_API_KEY: ${{ secrets.IFLOW_API_KEY }}
         run: |
           opencode run \
-            "Baca docs/blueprint.md, docs/roadmap.md, docs/bugs.md, docs/features.md.
+            "Baca docs/blueprint.md, docs/roadmap.md, docs/bug.md, docs/feature.md.
              Evaluasi prioritas.
              Update docs/task.md (OPEN / PRIORITY / ROLE / SCOPE).
              Jangan coding." \
@@ -260,7 +260,7 @@ jobs:
         run: |
           opencode run \
             "Scan log, PR, issue.
-             Catat bug baru ke docs/bugs.md.
+             Catat bug baru ke docs/bug.md.
              Jangan fix." \
             --agent reliability-engineer \
             --model opencode/deepseek-v4-flash-free \
diff --git a/.github/workflows/on-pull.yml b/.github/workflows/on-pull.yml
index 8c8dda26..9bda09fe 100644
--- a/.github/workflows/on-pull.yml
+++ b/.github/workflows/on-pull.yml
@@ -50,7 +50,7 @@ jobs:
         uses: actions/setup-node@v6
         continue-on-error: true
         with:
-          node-version-file: ".node-version"
+          node-version: 20
           cache: 'npm'
 
       - name: Configure Git
diff --git a/.github/workflows/parallel.yml b/.github/workflows/parallel.yml
index e94c70cd..225d7e93 100644
--- a/.github/workflows/parallel.yml
+++ b/.github/workflows/parallel.yml
@@ -67,7 +67,7 @@ jobs:
 
       - uses: actions/setup-node@v6
         with:
-          node-version-file: ".node-version"
+          node-version: "20"
 
       - run: npm ci || true
 
@@ -263,7 +263,7 @@ jobs:
 
       - uses: actions/setup-node@v6
         with:
-          node-version-file: ".node-version"
+          node-version: "20"
 
       - name: Install OpenCode
         run: |
@@ -341,7 +341,7 @@ jobs:
 
       - uses: actions/setup-node@v6
         with:
-          node-version-file: ".node-version"
+          node-version: "20"
 
       - run: npm ci || true
 
@@ -396,7 +396,7 @@ jobs:
 
       - uses: actions/setup-node@v6
         with:
-          node-version-file: ".node-version"
+          node-version: "20"
 
       - name: Install OpenCode
         run: |
diff --git a/.github/workflows/pr-gatekeeper.yml b/.github/workflows/pr-gatekeeper.yml
index b4819b8b..f9b6e1f6 100644
--- a/.github/workflows/pr-gatekeeper.yml
+++ b/.github/workflows/pr-gatekeeper.yml
@@ -28,7 +28,7 @@ jobs:
       - name: Setup Node.js
         uses: actions/setup-node@v6
         with:
-          node-version-file: ".node-version"
+          node-version: "20"
           cache: "npm"
 
       - name: Cache Build & Deps
